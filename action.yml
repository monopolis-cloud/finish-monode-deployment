name: 'Monopolis: Finish Monode Rollout'
description: 'Registers the end of a Monode rollout'
author: 'Monopolis'
branding:
  icon: 'code'
  color: 'gray-dark'
inputs:
  success:
    description: 'If this rollout succeded.'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'

